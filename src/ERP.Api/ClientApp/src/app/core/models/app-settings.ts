export interface AppSettings {
  isDarkTheme: boolean;
  isSideBarMini: boolean;
  // language: string;
}

export const defaults: AppSettings = {
  isDarkTheme: false,
  isSideBarMini: false,
  // language: 'es-MX',
};
